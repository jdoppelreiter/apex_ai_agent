{
  "name": "apex-process-tracker",
  "version": "1.0.0",
  "description": "WebSocket bridge between Oracle APEX (PL/SQL) and the browser for background process status updates.",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ws": "^8.17.0"
  },
  "engines": {
    "node": ">=18"
  }
}
